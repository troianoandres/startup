describe("Tests for TSignupController", function() {

  
  /*
  beforeEach(function() {
    angular.mock.module("AuthenticationModule");
    angular.mock.module("DatastoreModule");
    module("accessModule", ["AuthenticationModule", "DatastoreModule"]);
  });

  var controller, scope;

  beforeEach( 
    inject(function($controller, $rootScope) {
      scope = $rootScope;
      controller = $controller("TSignupController", {
        $scope: scope
      });
    })
  );
  */
  
 
  it("$scope should not be null", function() {
    expect(1).toBe(1);
  });
  

});